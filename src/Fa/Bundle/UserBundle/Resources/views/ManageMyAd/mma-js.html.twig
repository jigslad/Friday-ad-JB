<div id="markAdAsSoldOrExpireBulkModal" class="reveal-modal tiny remove-item-modal" data-options="close_on_background_click:false;close_on_esc:false;" data-reveal>
    <a class="close-reveal-modal">&#215;</a>
    <h5 class="modal-title">
        {{'Remove ad'|trans({}, 'frontend-manage-my-ad')}}
    </h5>
    <div class="modal-content remove-modal">
        <div class="row">
            <div class="columns">
                <h5>{{'Was your advert successful?'|trans({}, 'frontend-manage-my-ad')}}</h5>
            </div>
        </div>
        <div class="mt20 remove-item-btn">
            <div class="row">
                <div class="large-6 columns">
                    <button type="button" class="button secondary-btn-4 thin-btn expand" onclick="closeRevealModel('#markAdAsSoldOrExpireBulkModal');changeAdStatusBulk('{{constant('Fa\\Bundle\\EntityBundle\\Repository\\EntityRepository::AD_STATUS_SOLD_ID')}}');">{{'Yes'|trans({}, 'frontend-manage-my-ad')}}</button>
                </div>
                <div class="large-6 columns">
                    <button type="button" class="button secondary-btn-4 thin-btn expand" onclick="closeRevealModel('#markAdAsSoldOrExpireBulkModal');changeAdStatusBulk('{{constant('Fa\\Bundle\\EntityBundle\\Repository\\EntityRepository::AD_STATUS_EXPIRED_ID')}}');">{{'No'|trans({}, 'frontend-manage-my-ad')}}</button>
                </div>
                <div class="large-12 columns">
                    <button type="button" class="button secondary-btn-1 thin-btn expand" onclick="closeRemoveAdConfirmationBox('markAdAsSoldOrExpireBulkModal');">{{'Cancel'|trans({}, 'frontend-manage-my-ad')}}</button>
                </div>
            </div>
        </div>
    </div>
</div>          
                
<div id="markAdAsSoldOrExpireModal" class="reveal-modal tiny remove-item-modal" data-options="close_on_background_click:false;close_on_esc:false;" data-reveal>
    <a class="close-reveal-modal">&#215;</a>
    <h5 class="modal-title">
        {{'Remove ad'|trans({}, 'frontend-manage-my-ad')}}
    </h5>
    <div class="modal-content remove-modal">
        <div class="row">
            <div class="columns">
                <h5>{{'Was your advert successful?'|trans({}, 'frontend-manage-my-ad')}}</h5>
            </div>
        </div>
        <div class="mt20 remove-item-btn">
            <div class="row">
                <div class="large-6 columns">
                    <button type="button" class="button secondary-btn-4 thin-btn expand" onclick="closeRevealModel('#markAdAsSoldOrExpireModal');changeAdStatus('{{constant('Fa\\Bundle\\EntityBundle\\Repository\\EntityRepository::AD_STATUS_SOLD_ID')}}');">{{'Yes'|trans({}, 'frontend-manage-my-ad')}}</button>
                </div>
                <div class="large-6 columns">
                    <button type="button" class="button secondary-btn-4 thin-btn expand" onclick="closeRevealModel('#markAdAsSoldOrExpireModal');changeAdStatus('{{constant('Fa\\Bundle\\EntityBundle\\Repository\\EntityRepository::AD_STATUS_EXPIRED_ID')}}');">{{'No'|trans({}, 'frontend-manage-my-ad')}}</button>
                </div>
                <div class="large-12 columns">
                    <button type="button" class="button secondary-btn-1 thin-btn expand" onclick="closeRemoveAdConfirmationBox('markAdAsSoldOrExpireModal');">{{'Cancel'|trans({}, 'frontend-manage-my-ad')}}</button>
                </div>
            </div>
        </div>
    </div>
</div>




<div id="markAdAsUnBoostModal" class="reveal-modal tiny remove-item-modal" data-options="close_on_background_click:false;close_on_esc:false;" data-reveal>
    <a class="close-reveal-modal">&#215;</a>
    <h5 class="modal-title">
        {{'Boost ads'|trans({}, 'frontend-manage-my-ad')}}
    </h5>
    <div class="modal-content remove-modal">
        <div class="row">
            <div class="columns">
                <h5>{{'Do you really want to unboost all your selected ads?'|trans({}, 'frontend-manage-my-ad')}}</h5>
            </div>
        </div>
        <div class="mt20 remove-item-btn">
            <div class="row">
                <div class="large-12 columns">
                    <button type="button" class="button secondary-btn-4 thin-btn expand" onclick="closeRevealModel('#markAdAsUnBoostModal');unBoostSelectedAds();">{{'Yes'|trans({}, 'frontend-manage-my-ad')}}</button>
                </div>
                <div class="large-12 columns">
                    <button type="button" class="button secondary-btn-1 thin-btn expand" onclick="closeRemoveAdConfirmationBox('markAdAsUnBoostModal');">{{'Cancel'|trans({}, 'frontend-manage-my-ad')}}</button>
                </div>
            </div>
        </div>
    </div>
</div>

<div id="markAdAsInactiveModal" class="reveal-modal tiny remove-item-modal" data-options="close_on_background_click:false;close_on_esc:false;" data-reveal>
    <a class="close-reveal-modal">&#215;</a>
    <h5 class="modal-title">
        {{'Remove ad'|trans({}, 'frontend-manage-my-ad')}}
    </h5>
    <div class="modal-content remove-modal">
        <div class="row">
            <div class="columns">
                <h5>{{'Are you sure that you want to permanently delete this advert?'|trans({}, 'frontend-manage-my-ad')}}</h5>
            </div>
        </div>
        <div class="mt20 remove-item-btn">
            <div class="row">
                <div class="large-12 columns">
                    <button type="button" class="button secondary-btn-4 thin-btn expand" onclick="closeRevealModel('#markAdAsInactiveModal');changeAdStatus('{{constant('Fa\\Bundle\\EntityBundle\\Repository\\EntityRepository::AD_STATUS_INACTIVE_ID')}}');">{{'Delete'|trans({}, 'frontend-manage-my-ad')}}</button>
                </div>
                <div class="large-12 columns">
                    <button type="button" class="button secondary-btn-1 thin-btn expand" onclick="closeRemoveAdConfirmationBox('markAdAsInactiveModal');">{{'Cancel'|trans({}, 'frontend-manage-my-ad')}}</button>
                </div>
            </div>
        </div>
    </div>
</div>

<div id="socialShareMobileModal" class="reveal-modal tiny" data-options="close_on_background_click:false;close_on_esc:false;" data-reveal >
    <a class="close-reveal-modal">&#215;</a>
    <h5 class="modal-title">
        {{'Share this ad'|trans({}, 'frontend-manage-my-ad')}}
    </h5>
    <div class="modal-content remove-modal">
        <div class="row">
            <div class="columns">
                <div id="social_share_icon_div" class="dtl-social-icons manage-list-social-icons text-left"></div>
            </div>
        </div>
    </div>
</div>


<div id="featuredOffConfirmModal" class="reveal-modal tiny remove-item-modal mini-modal-box"  data-options="close_on_background_click:false;close_on_esc:false;" data-reveal>
    <a class="close-reveal-modal closeFeaturedUpsell" onclick="turnOnFeaturedToggle();closeRevealModel('#featuredOffConfirmModal');">&#215;</a>
    <h5 class="modal-title"> 
        {{'Are you sure?'|trans({}, 'frontend-manage-my-ad')}}
    </h5>
    <div class="modal-content remove-modal">
        <div class="row">
            <div class="columns">
                <h5>You have paid to Feature this Ad and it still has another <span id="leftdaysforexpiry"></span> days left of being featured.<br>Are you sure you wish to remove the Feature option?</h5>
            </div>
        </div>
        <div class="mt20 remove-item-btn">
            <div class="row">                
                <div class="large-12 columns">
                    <button type="button" class="button secondary-btn-4 thin-btn expand" onclick="deactivateFeatured();">{{'End Feature'|trans({}, 'frontend-manage-my-ad')}}</button>
                </div>
                <div class="large-12 columns">
                    <button type="button" class="button secondary-btn-1 thin-btn expand closeFeaturedUpsell" onclick="turnOnFeaturedToggle();closeRevealModel('#featuredOffConfirmModal');">{{'Cancel'|trans({}, 'frontend-manage-my-ad')}}</button>
                </div>
            </div>
        </div>
    </div>
</div>

<div id="creditConfimBox" class="reveal-modal tiny remove-item-modal mini-modal-box" data-options="close_on_background_click:false;close_on_esc:false;" data-reveal>
    <a class="close-reveal-modal">&#215;</a>
    <h5 class="modal-title">
        {{'Use credit'|trans({}, 'frontend-ad-package')}}
    </h5>
    <div class="modal-content remove-modal">
        <div class="row">
            <div class="columns">
                <h5>{{'Are you sure you want to use a credit?'|trans({}, 'frontend-ad-package')}}</h5>
                <div class="mt15" id="remaining_credit_text"></div>
            </div>
        </div>
        <div class="mt20 remove-item-btn">
            <div class="row">
                <div class="large-12 columns">
                    <button type="button" class="button secondary-btn-4 thin-btn expand" onclick="closeCreditBox('creditConfimBox');">{{'Cancel'|trans({}, 'frontend-ad-package')}}</button>
                </div>
                <div class="large-12 columns">
                    <button type="button" class="button secondary-btn-1 thin-btn expand" onclick="closeRevealModel('#creditConfimBox');selectCredit();">{{'Continue'|trans({}, 'frontend-ad-package')}}</button>
                </div>
            </div>
        </div>
    </div>
</div>

<div id="unsufficientCreditBox" class="reveal-modal tiny remove-item-modal mini-modal-box" data-options="close_on_background_click:false;close_on_esc:false;" data-reveal>
    <a class="close-reveal-modal">&#215;</a>
    <h5 class="modal-title">
        {{'Use credit'|trans({}, 'frontend-ad-package')}}
    </h5>
    <div class="modal-content remove-modal">
        <div class="row">
            <div class="columns">
                <h5>{{'You have insufficient credits.'|trans({}, 'frontend-ad-package')}}</h5>
                <div class="mt15" >{{'Sorry! You only have enough credits.'|trans({}, 'frontend-ad-package')|raw}}</div>
            </div>
        </div>
    </div>
</div>
    
<div id="featuredTopUpsellModal" data-options="close_on_background_click:false;close_on_esc:false;" class="ga-featuredUnavailable medium reveal-modal remove-item-modal mma-featured-pop" data-reveal="" data-selfeaturedid=""></div>

<script language="javascript" type="text/javascript">
//<![CDATA[
    var adIdToRemove = '';
    var statusIdToChange = '';
    var featurredUpsellRemoveId = '';
    var featurredUpsellRemoveAttrId = '';
    var featuredUpsellId = '';
    var adIdsArray = "{{ adIdArray|join(',') }}";
    
    function removeAdConfirmation(adId, currentAdStatus)
    {
    	adIdToRemove = adId;
        if (currentAdStatus == "{{constant('Fa\\Bundle\\EntityBundle\\Repository\\EntityRepository::AD_STATUS_LIVE_ID')}}") {
        	$('#markAdAsSoldOrExpireModal').foundation('reveal', 'open');
        } else {
        	$('#markAdAsInactiveModal').foundation('reveal', 'open');
        }
    }

    function closeRemoveAdConfirmationBox(modelId)
    {
    	adIdToRemove = '';
    	statusIdToChange = '';
    	closeRevealModel('#'+modelId);
    }

    function openMarkAdAsBoostedModal() {
        var selectedBoostAdCnt = 0;var msgInsideModal = '';
        var remainingCnt = {{(boostAdRemaining is defined and boostAdRemaining!='')?boostAdRemaining:0}};
        var remainingCntAfterSelection = 0;

        if(checkAllBoostButtonEnabled()) {                  
            $('.boost_your_ad').removeClass('ajaxboost_your_ad');      
            $(".boostad").each(function(){
                if($(this).parent('span').hasClass('checked')) {
                    selectedBoostAdCnt = selectedBoostAdCnt+1;
                }
             });
            remainingCntAfterSelection = remainingCnt - selectedBoostAdCnt;
            /*if(selectedBoostAdCnt > remainingCnt) {
            	alert('Sorry, your maximum boost count has been exceeded.');
                //alert('Sorry, your maximum boost count has been exceeded. Please select only '+ remainingCnt +' adverts ');
                $(".boostad_selectall").parent('span').removeClass('checked');
                $(".boostad").parent('span').removeClass('checked');
                $(".boostad_selectall").parent('span').trigger("change");   
                $('#markAdAsBoostedModal').hide();
                $('.boost_your_ad').addClass('ajaxboost_your_ad');
                $("#markAdAsBoostedModal").removeClass('manage-my-ads-list-pad-bot');             
            } else*/

            if(selectedBoostAdCnt==0) {
                $('#markAdAsBoostedModal').hide();
                $('.boost_your_ad').addClass('ajaxboost_your_ad');
                $("#markAdAsBoostedModal").removeClass('manage-my-ads-list-pad-bot');
            } else {
                $('.selAdsCount').html(selectedBoostAdCnt);
                //$('.leftoutAdCount').html(remainingCntAfterSelection);
                $('#markAdAsBoostedModal').show();
                $('.boost_your_ad').removeClass('ajaxboost_your_ad');
                $("#markAdAsBoostedModal").addClass('manage-my-ads-list-pad-bot');
                /*$('html, body').animate({
                    scrollTop: $("#markAdAsBoostedModal").offset().top
                }, 2000);*/
            }
        }
    }
    function openMarkAdAsUnBoostModal() {
        $('#markAdAsUnBoostModal').foundation('reveal', 'open');
    }

    function checkAllBoostButtonEnabled() {
        var disableCnt = 0;
    	$(".boost_your_ad").each(function(){
    		if($(this).is('[disabled=disabled]')) {
    			disableCnt = disableCnt+1;
            }
    	});
    	if(disableCnt>0) { return false; }
    	else { return true; }    	
    }
    
    var ajaxLoading = false;
    function changeAdStatus(statusId)
    {
    	statusIdToChange = statusId;
        if (!ajaxLoading && adIdToRemove != '') {
        	ajaxLoading = true;
        	blockPage();
        	blockElement('.db-right-part');
        	$.ajax({
                type: "POST",
                url: '{{path("ajax_manage_my_ads_change_status")}}',
                data: {'adId':adIdToRemove, 'newStatusId':statusIdToChange},
                cache:false,
                asyn:false
            })
            .always(function(response) {
                unblockPage();
             })
            .done(function(response) {
                location.reload();
            });
        }
    }
    
    var ajaxChangeAdStatusBulkLoading = false;
    function changeAdStatusBulk(statusId)
    {
    	statusIdToChange = statusId;
    	$('#stop-sel-ads-btn').prop("disabled", true);
        var selectedStopAdIds = '';
        $(".boostad").each(function(){
            if($(this).parent('span').hasClass('checked')) {
                selectedStopAdIds += $(this).val()+',';
            }
        });
        if (!ajaxLoading && selectedStopAdIds != '') {
        	ajaxChangeAdStatusBulkLoading = true;
        	blockPage();
        	blockElement('.db-right-part');
        	$.ajax({
                type: "POST",
                url: '{{path("ajax_manage_my_ads_stop_multiple_ad")}}',
                data: {'adIds':selectedStopAdIds, 'newStatusId':statusIdToChange},
                cache:false,
                asyn:false
            })
            .always(function(response) {
                unblockPage();
             })
            .done(function(response) {
            	window.location = '{{ path('manage_my_ads_active') }}';
            });
        }
    }

    function turnOnFeaturedToggle()
    {
    	if (featurredUpsellRemoveAttrId != '') {
        	$('#'+featurredUpsellRemoveAttrId).addClass('toggle-on').removeClass('toggle-off');
    	}
    }
    function deactivateFeatured()
    {
        if (!ajaxLoading && featurredUpsellRemoveId != '') {
        	ajaxLoading = true;
        	blockPage();
        	blockElement('.db-right-part');
        	closeRevealModel('#featuredOffConfirmModal');
        	$.ajax({
                type: "POST",
                url: '{{path("ajax_manage_my_ads_deactivate_featured")}}',
                data: {'adId':featurredUpsellRemoveId, 'upsellId':featuredUpsellId },
                cache:false,
                asyn:false
            })
            .always(function(response) {
                unblockPage();
             })
            .done(function(response) {
            	$('#'+featurredUpsellRemoveAttrId).removeClass('toggle-on').addClass('toggle-off');
            	var featuredNorCnt = $('.featuredCntNormal').html();
            	var reducedFeatNorCnt = featuredNorCnt-1;
                $('.featuredCntNormal').html(reducedFeatNorCnt);
            });
        }
    }

    function openSocialSharePopup(adId, adUrl, adTitle)
    {
        if (adUrl != '' && adTitle != '') {
        	blockPage();
        	blockElement('.db-right-part');
        	$.ajax({
                type: "POST",
                url: '{{path("manage_my_ads_social_share_popup")}}',
                data: {'adId':adId, 'adUrl':adUrl, 'adTitle':adTitle}
            })
            .always(function(response) {
                unblockPage();
             })
            .done(function(response) {
                $('#social_share_icon_div').html(response.htmlContent);
            	$('#socialShareMobileModal').foundation('reveal', 'open');
            });
        }
    }

    function getAdsStatus(adIdArray)
    {
    	$.ajax({
            type: "POST",
            url: '{{path("ajax_manage_my_ads_get_status")}}',
            data: {'adIds':adIdArray, 'type': '{{app.request.get('type')}}'}
        })
        .done(function(response) {
            $.each (response, function (key, value) {
                /*$('#item_status_'+key).removeAttr("class");
                $('#item_status_'+key).addClass("item-status "+value.class);
                $('#item_status_'+key).html(value.status);
                if (value.status == 'Live') {
                    if ($('#item_detail_link_'+key).children('a').length < 1) {
                        var item_title = $('#item_detail_link_'+key).html();
                        var item_url   = Routing.generate('ad_detail_page_by_id', {'id':key});
                        item_url = item_url.replace(':https','');
                        $('#item_detail_link_'+key).html('<a href="'+item_url+'" target="_blank">'+item_title+'</a>');
                    }
                }*/
                $('#item_card_'+key).html(value);
            });
            window.location = '{{path("manage_my_ads_active")}}';
        });
    }

    $('#select-featureds').change(function(e) {
		var selFeaturedId = $('#select-featureds').val();
		var setType = "{{app.request.get('type')}}";

    	$.ajax({
            type: "POST",
            url: '{{path("ajax_manage_my_ads_get_featured_ads")}}',
            data: {'selAdsOption': selFeaturedId }
        })
        .done(function(response) {
            location.reload();
        	/*$('#resulthtml_div').html(response);*/
        }); 
    });
    
    $(document).on("click", '.closeFeaturedUpsell', function(event) {
    	var getFeaturedId =  $('#featuredTopUpsellModal').data('selfeaturedid'); 
    	$('#'+getFeaturedId).removeClass('toggle-on').addClass('toggle-off');
    });
    
     $('#manage-my-ads-list-only').on('click','.ajaxboost_your_ad', function(e)
        {
            var boost_value = 1;
            var elem_id = $(this).attr('id');
            var split_ad_id = elem_id.split("_");
            var adIdToBoost = split_ad_id[split_ad_id.length-1];
            if($('#markAdAsBoostedModal').css('display')=='none') {
                $('#'+elem_id).attr("disabled", true);
                e.stopPropagation();
                $('#'+elem_id).off('click','#manage-my-ads-list-only');
                $('.boost_your_ad').removeClass('ajaxboost_your_ad');
                $.ajax({
                        type: "POST",
                        url: '{{path("ajax_manage_my_ads_boost_ad")}}',
                        data: {'adId':adIdToBoost,boost_value:boost_value},
                        asyn:false,
                        cache:false,
                        success:function(result)
                        {
                            if(result.error == "")
                            {
                                window.dataLayer = window.dataLayer || []
                                dataLayer.push({
                                    'event' : 'Options',
                                    'eventCategory':  'Options',
                                    'eventAction': 'Boost Button',
                                    'eventLabel': adIdToBoost+' - '+result.gaStr
                                });
                                ga('send','event','MMA','Boost Button', result.gaStr);
                            }
                        },
                        complete: function(){
                            window.location.href = '{{ path('manage_my_ads_active') }}';
                        }
                    });
            }
        });

    $('#manage-my-ads-list-only').on('click','.ajaxcustom-checkbox', function() {
        if(checkAllBoostButtonEnabled()) {
            if($(".boostad").length == $(".boostad:checked").length) {
                $(".boostad_selectall").attr("checked", "checked");
                $('.boostad_selectall').parent('span').addClass('checked');
                $("#boostad_selectall_inmodal").attr("checked", "checked");
                $('#boostad_selectall_inmodal').parent('span').addClass('checked');
            } else if($(".boostad").length > $(".boostad:checked").length) {
                $("#boostad_selectall_inmodal").attr("checked", "checked");
                $('#boostad_selectall_inmodal').parent('span').addClass('checked');
                $(".boostad_selectall").removeAttr("checked");
                $('.boostad_selectall').parent('span').removeClass('checked');
            } else {
                $(".boostad_selectall").removeAttr("checked");
                $('.boostad_selectall').parent('span').removeClass('checked');
                $("#boostad_selectall_inmodal").removeAttr("checked");
                $('#boostad_selectall_inmodal').parent('span').removeClass('checked');
            }
            openMarkAdAsBoostedModal();
        } else {
            if ($(this).hasClass('checked')) {
                $(this).removeClass('checked');
                $(this).find('input[type="checkbox"]:first').removeAttr('checked');
                
            }
        }
    });

    {% if showCompetitionPopup is defined and showCompetitionPopup %}
        function enterInCompetition()
        {
            blockPage();
            $.ajax({
                type: "GET",
                async: true,
                url : '{{ path('enter_in_competition', {'transaction_id': app.request.get('transaction_id')}) }}',
            })
            .always(function(response) {
                unblockPage();
            })
            .done(function(response) {
                hideAlertMessage();
                if (response.htmlContent.length) {
                    $('#competitionModel').html(response.htmlContent);
                    $('#competitionModel').foundation('reveal', 'open', {
                        close_on_background_click : false,
                        close_on_esc : false
                    });
                } else if (response.error.length) {
                    $('.manage-item-tabs').prepend(decorateMessage(response.error, 'alert'));
                    scrollToElement('.manage-item-tabs', '1000', -150);
                }
            });
        }
    {% endif %}

    $(document).ready(function() {
    	{% if showCompetitionPopup is defined and showCompetitionPopup %}
    	    enterInCompetition();
    	{% endif %}
        {% if adIdArray|length and getTranscationJs is defined %}
            setTimeout(function() {   //calls click event after a certain time
                getAdsStatus('{{ adIdArray|join(',') }}');
            }, 3000);
        {% endif %}
    });
//]]>
</script>
<script>
(function($) {
    $.fn.extend({
    	toggleSwitch: function(){
            $('.featured-indiv-upsell').on('click',function(){
              var elem_id = $(this).attr('id');
              var split_ad_id = elem_id.split("_");
              var adId = split_ad_id[1];
              var upsellId = split_ad_id[2];
              var catupsellId = split_ad_id[3];
              var leftDaysforExpire = split_ad_id[4];
              if($(this).hasClass('toggle-off')){
                $(this).removeClass('toggle-off').addClass('toggle-on');
                $('#featuredTopUpsellModal').data('selfeaturedid', elem_id);
                featuredTopUpsellBtn(adId,upsellId, catupsellId);                 	             	
              }else{
            	 featurredUpsellRemoveId = adId;
            	 featuredUpsellId = upsellId;
            	 featurredUpsellRemoveAttrId = elem_id;
            	 $('#leftdaysforexpiry').html(leftDaysforExpire);
            	 $('#featuredOffConfirmModal').foundation('reveal', 'open');
                 $(this).removeClass('toggle-on').addClass('toggle-off'); 
                 gaEvent(adId,'','Featured Off');
              }
            });

            $('.netsuite-featured-indiv-upsell').on('click',function(){
                var elem_id = $(this).attr('id');
                var split_ad_id = elem_id.split("_");
                var adId = split_ad_id[1];
                var upsellId = split_ad_id[2];
                var catupsellId = split_ad_id[3];
                var rootCatId = split_ad_id[3];
                if($(this).hasClass('toggle-off')){
                  $(this).removeClass('toggle-off').addClass('toggle-on');
                  $('#featuredTopUpsellModal').data('selfeaturedid', elem_id);
                  {% if remainingFeaturedCredits > 0 %}                	
                     useCreditForFeaturedPackage(adId,upsellId);
                  {% else %}
                  	featuredTopPackageBtn(adId,rootCatId);                 	             	
                  {% endif %}
                }else{
              	 featurredUpsellRemoveId = adId;
              	 featuredUpsellId = upsellId;

              	 $('#featuredOffConfirmModal').foundation('reveal', 'open');
                 $(this).removeClass('toggle-on').addClass('toggle-off'); 
                }
              })
        }
    })
})(jQuery)
$(document).ready(function(){	
    $('.featured-switch').toggleSwitch();
 });
</script>
