<div class="row">
     <div class="columns">
         <div class="outline">
             <p class="instruction">{{ 'Click on up and down arrow to sort result'|trans({}, 'messages') }}</p>
             <table class="table table-striped table-bordered table-hover">
                 <thead class="mb0">
                     <tr>
                         <th width="30%">
                            {{ sortable_link('static_block_admin', 'static_block__name', 'Name'|trans, sorter) }}
                         </th>
                         <th width="30%">
                            {{ 'Status'|trans({}, 'messages') }}
                         </th>
                         <th width="10%"><i class="fa fa-gear"></i> {{ 'Action'|trans({}, 'messages') }}</th>
                     </tr>
                 </thead>
                 <tbody>
                     {% if pagination.getNbResults() %}
                          {% for staticBlock in pagination.getCurrentPageResults() %}
                          <tr class="gradeX">
                              <td>{{ staticBlock.name }}</td>
                              <td>{{ statusArray[staticBlock.status] }}</td>
                              <td>
                                  <button href="#" data-dropdown="drop{{ staticBlock.id }}" aria-controls="drop1" aria-expanded="false" class="small button dropdown tools_btn">Select</button>
                                  <br />
                                  <ul id="drop{{ staticBlock.id }}" data-dropdown-content class="f-dropdown" aria-hidden="true" tabindex="-1">
	                                  {% if staticBlockEditGrant %}
	                                      <li><a href="{{ path('static_block_edit_admin', {'id': staticBlock.id}) }}"><i class="fi-pencil"></i> {{ 'Edit'|trans({}, 'messages') }}</a></li>
	                                  {% endif %}

	                                  {% if staticBlockDeleteGrant %}
	                                      <li><a href="{{ path('static_block_delete_admin', {'id': staticBlock.id}) }}" onclick="javascript:return confirm('{{ 'Do you want to proceed deleting this record?'|trans({}, 'messages') }}')"><i class="fi-trash"></i> {{ 'Delete'|trans({}, 'messages') }}</a></li>
	                                  {% endif %}
                                  </ul>
                              </td>
                          </tr>
                          {% endfor %}
                     {% else %}
                          <tr class="odd gradeX">
                              <td colspan="4" align="center">{{ 'No record found'|trans({}, 'messages') }}</td>
                          </tr>
                     {% endif %}
                 </tbody>
             </table>
        </div>
    </div>
</div>