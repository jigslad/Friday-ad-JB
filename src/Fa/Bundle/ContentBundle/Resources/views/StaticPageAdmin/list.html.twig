<div class="row">
     <div class="columns">
         <div class="outline">
             <p class="instruction">{{ 'Click on up and down arrow to sort result'|trans({}, 'messages') }}</p>
             <table class="table table-striped table-bordered table-hover">
                 <thead class="mb0">
                     <tr>
                         <th width="20%">
                            {{ sortable_link('static_page_admin', 'static_page__title', 'Title'|trans, sorter) }}
                         </th>
                         <th width="20%">
                            {{ 'Status'|trans({}, 'messages') }}
                         </th>
                         <th width="10%"><i class="fa fa-gear"></i> {{ 'Action'|trans({}, 'messages') }}</th>
                     </tr>
                 </thead>
                 <tbody>
                     {% if pagination.getNbResults() %}
                          {% for staticPage in pagination.getCurrentPageResults() %}
                          <tr class="gradeX">
                              <td>{{ staticPage.title }}</td>
                              <td>{{ statusArray[staticPage.status] }}</td>
                              <td>
                                  <button href="#" data-dropdown="drop{{ staticPage.id }}" aria-controls="drop1" aria-expanded="false" class="small button dropdown tools_btn">Select</button>
                                  <br />
                                  <ul id="drop{{ staticPage.id }}" data-dropdown-content class="f-dropdown" aria-hidden="true" tabindex="-1">
	                                  {% if staticPageEditGrant %}
	                                      <li><a href="{{ path('static_page_edit_admin', {'id': staticPage.id}) }}"><i class="fi-pencil"></i> {{ 'Edit'|trans({}, 'messages') }}</a></li>
	                                  {% endif %}

	                                  {% if staticPageDeleteGrant %}
	                                      <li><a href="{{ path('static_page_delete_admin', {'id': staticPage.id}) }}" onclick="javascript:return confirm('{{ 'Do you want to proceed deleting this record?'|trans({}, 'messages') }}')"><i class="fi-trash"></i> {{ 'Delete'|trans({}, 'messages') }}</a></li>
	                                  {% endif %}
                                  </ul>
                              </td>
                          </tr>
                          {% endfor %}
                     {% else %}
                          <tr class="odd gradeX">
                              <td colspan="4" align="center">{{ 'No record found'|trans({}, 'messages') }}</td>
                          </tr>
                     {% endif %}
                 </tbody>
             </table>
        </div>
    </div>
</div>